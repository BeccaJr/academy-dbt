version: 2

models:
  - name: dim_cartoes
    description: Essa é a dimensão de Cartões.
    columns:
      - name: cartao_sk
        description: Essa é a chave Surrogate key da tabela
        tests:
          - unique
          - not_null