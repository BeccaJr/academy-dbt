version: 2

models:
  - name: dim_clientes
    description: Essa é a dimensão de datas.
    columns:
      - name: cliente_sk
        description: Essa é a chave Surrogate key da tabela
        tests:
          - unique
          - not_null